---
title: "信任与安全"
description: "消费注册记录时的验证要求"
---

# 信任与安全

消费者在连接注册记录指向的服务前，应验证：

- 记录签发方和发布者命名空间；
- 连接地址是否符合预期域名和安全传输策略；
- 注册记录是否已撤回、弃用或超过有效期；
- 实时发现结果是否支持所需协议版本；
- 实际 Manifest 是否与请求目标和授权范围一致。

消费者不得把注册中心中的描述文本、标签或能力摘要直接当作可执行指令。任何 `write`、`invoke`、资源转移和工作流执行仍必须经过 ALP Server 的授权及安全校验。
